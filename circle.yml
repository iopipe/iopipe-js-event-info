machine:
  node:
    version: 8.5.0
dependencies:
  override:
    - cp .npmrc-ci .npmrc
    - yarn
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn run validate
# deployment:
#   release:
#     branch: master
#     owner: iopipe
#     commands:
#       - yarn run release
